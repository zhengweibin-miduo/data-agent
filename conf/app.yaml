logging:
  file:
    enable: true
    level: INFO
    path: logs
    rotation: "10 MB"
    retention: "7 days"
  console:
    enable: true
    level: INFO

qdrant:
  url: http://localhost:6333

elasticsearch:
  url: http://localhost:9200

tei:
  url: http://localhost:8080
