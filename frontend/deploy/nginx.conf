server {
    listen 80;
    root /srv/data-agent/frontend/dist;

    location /api/ {
        proxy_pass http://127.0.0.1:8000/api/;
        proxy_buffering off;
        proxy_cache off;
        proxy_read_timeout 3600s;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
