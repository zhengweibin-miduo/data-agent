# 删除审查回复内部标签防御说明

## Goal

删除已经没有生成端或解析端依赖的审查回复内部标签防御说明，继续依靠现有
“已修复 / 无需修改 / 无法安全完成”固定回复模板约束对外输出。

## Requirements

- 从 `code_review.md` 删除禁止 `[裁决]`、`SHOULD_FIX` 等内部标签的条目。
- 从 Codex 审查委派提示词删除同一禁止说明。
- 删除只验证该说明存在的脚本测试断言。
- 不修改三个固定回复模板、审查优先级、thread resolve 规则或委派行为。
- 提交并普通推送到 PR #71 原 head 分支，不新建 PR、不 force-push。

## Acceptance Criteria

- [x] 有效工作流代码与规范不再包含 `[裁决]` 或 `SHOULD_FIX` 标签约束。
- [x] Codex 审查委派脚本内置测试通过。
- [x] Git diff 仅包含两处说明、对应断言及本任务记录。
- [ ] 改动提交并推送到 PR #71 原分支。

## Notes

- 仓库历史任务记录中的旧标签说明属于审计历史，不在本次删除范围内。
- 用户要求暂不推送；本地提交完成后保留任务，等待后续明确恢复推送。
