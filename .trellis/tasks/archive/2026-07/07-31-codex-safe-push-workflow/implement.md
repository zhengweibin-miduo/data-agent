# 实施计划

1. 在项目 `git-pr-rules` 中加入远端干预状态快照、祖先关系判定矩阵、
   未发布提交例外、停止条件和普通 push 竞态处理。
2. 更新 `.trellis/workflow.md` Phase 3.4：
   - 保留任务文件分类和一次性 commit plan；
   - 已有明确 commit / push 授权时无需重复确认；
   - commit 后、push 前必须执行远端干预判定；
   - 只在安全门禁通过时直接推送。
3. 更新三个 GitHub 委派提示词，让受委派 Codex：
   - 先读取根目录规则和项目 `git-pr-rules`；
   - 对 head 变化执行统一判定；
   - 远端线性前进可安全同步继续；
   - 分叉、冲突、未知范围或需要强推时停止。
4. 更新三个脚本的内置测试断言，验证新提示词同时包含允许条件和停止条件。
5. 运行脚本自测、仓库相关 lint/格式检查和 Trellis check；复核工作区范围。
6. 按已授权流程提交，在 push 前重新 fetch 并应用判定矩阵；普通推送到
   PR #71 原分支，不创建新 PR、不 force-push。
