# Codex 代码审查修复模板

> 将下列内容复制给 Codex，并填写尖括号中的信息。未填写的授权一律视为未授权。

## 任务信息

- PR：`<PR URL 或编号；无则写“无”>`
- 目标分支 / 当前分支：`<base> / <head>`
- 审查意见来源：`<Codex GitHub Review、人工评论、CI 日志等>`
- 审查意见：`<粘贴评论、链接或文件>`
- 允许操作：`<仅本地修改 / 允许 commit / 允许 push / 允许发布 PR 评论 / 允许解决审查线程 / 允许更新已有 PR / 允许创建 PR>`
- 补充限制：`<影响范围、兼容性要求、禁止修改项等>`

## 目标

核验并闭环处理上述审查意见。不要默认审查意见正确；只修复经代码、运行路径或测试证据确认有效的问题。

## 执行要求

### 1. 读取规则与上下文

开始前必须读取并遵守：

1. 仓库根目录 `AGENTS.md`。
2. 仓库根目录 `code_review.md` 中的问题证据、优先级和验证约定；其中“只审查，不修改”仅适用于审查阶段，修复阶段以本模板填写的“允许操作”为准。
3. 本次涉及目录对应的 `.trellis/spec/` 规范与项目工作流。
4. 涉及 Git、分支、commit、push 或 PR 时，读取 `.agents/skills/git-pr-rules/SKILL.md`。
5. 检查当前分支、工作区已有改动、PR 的 base/head、实际 diff 和相关测试；保护用户已有改动。

如其他规则与本模板的授权边界冲突，按仓库规则规定的优先级处理，并明确说明冲突。

### 2. 逐条核验审查意见

为每条意见分配编号，定位到具体文件、行、数据流或运行路径，并给出可复查证据。每条意见只能归入以下结论之一：

- `有效`：当前代码可复现，或可由明确的静态/运行证据证明。
- `误报`：审查前提与实际代码、依赖、配置或运行路径不符。
- `已修复`：当前分支已不存在该问题，并能指出对应改动或验证结果。
- `无法复现`：已按合理方式核验，但证据不足以确认问题存在。
- `阻塞`：缺少必要权限、依赖、环境、输入或外部状态，无法继续确认。

不得仅复述审查意见作为证据。对 `误报`、`已修复`、`无法复现` 或 `阻塞` 的意见，不要为了“看起来有改动”而修改代码。

### 3. 修复确认有效的问题

- 只修改解决已确认问题所必需的代码，保持最小 diff。
- 优先修复根因，并为缺陷补充能复现问题的回归测试。
- 不得删除或放宽类型标注、断言、测试、输入校验、错误处理、权限检查或安全边界来换取检查通过。
- 不得顺手重构、格式化、升级依赖或修改与审查意见无关的内容。
- 保持现有公开接口和兼容性；确需改变时，先说明影响并取得授权。
- 遇到阻塞时停止相关修改，记录已核验内容和解除阻塞所需条件，不猜测修复。

### 4. 验证

先执行与改动直接相关的最小测试，再执行仓库规定的完整检查。使用锁文件中的依赖和项目内已锁定的工具：

```powershell
uv sync --locked
uv lock --check
uv run ruff check app app_test
uv run pyright app app_test
uv run python -m compileall -q app app_test main.py
uv run python -m app.conf.app_config
uv run python -m app_test.client.test_mysql_client_manager
git diff --check
```

根据改动补充并执行相关测试，例如：

```powershell
uv run python -m <相关测试模块>
```

- 不得使用 `uv run --with ...` 临时访问 PyPI，也不得临时替换 Ruff、Pyright 或其他审查工具版本。
- 不得因检查失败而删除测试或弱化校验。
- MySQL 集成检查需要可用的服务和凭据；服务不可用时，按环境阻塞报告，不得声称通过。
- 命令无法执行时，保留错误原文并说明是代码失败还是环境阻塞；未执行的检查不得声称通过。
- 最后复查 `git diff`，确认没有无关改动、敏感信息或意外生成文件。

### 5. Git 与 PR 授权边界

默认只允许本地修改和只读检查。只有“允许操作”明确包含相应动作时，才可以执行 `commit`、`push` 或 PR 操作；授权不得自动向后续动作扩张。

- 未明确允许 `commit`：不得暂存或提交。
- 未明确允许 `push`：不得推送或修改远端分支。
- 未明确允许发布 PR 评论：不得发布评论。
- 未明确允许解决审查线程：不得解决或重新打开审查线程。
- 未明确允许更新已有 PR：不得修改已有 PR 的标题、描述、base、状态或其他元数据。
- 未明确允许创建 PR：不得创建新 PR；“允许更新已有 PR”不包含创建权限。
- 获得授权后仍须遵守 `git-pr-rules`，并在执行前核对实际分支名、远端以及 PR 的 base/head。

## 输出格式

全程使用简体中文；代码标识符、文件路径、命令、配置键、日志和错误原文保留英文。

### 审查意见闭环

| 编号 | 审查意见 | 结论 | 核验证据 | 实际改动 | 验证结果 |
| --- | --- | --- | --- | --- | --- |
| 1 | `<意见摘要>` | `<有效/误报/已修复/无法复现/阻塞>` | `<文件、行、调用链、测试或日志>` | `<改动文件与内容；未改则写“无”>` | `<命令及真实结果>` |

### 交付摘要

- 修改文件：`<文件列表；无则写“无”>`
- 新增/更新测试：`<测试列表；无则说明原因>`
- 验证结果：`<逐项列出通过、失败或未执行>`
- 提交与 PR 状态：`<未提交 / commit SHA / 已 push / PR 已更新>`
- 剩余风险或阻塞：`<内容；无则写“无已知风险”>`
