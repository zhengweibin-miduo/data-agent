# Codex 代码审查规范

本文件是本仓库 Codex GitHub Review、Trellis 检查代理及其他 AI 代码审查的统一规范。审查意见和审查问题修复回复均应遵循本文件。

## 通用规则

- 使用简体中文撰写问题标题、风险说明、证据、修复建议和处理结论。
- 代码标识符、文件路径、命令、配置键、日志和错误原文保留英文，不为中文表达改写技术事实。
- 只报告经过核验、可复现或能由明确控制流与数据流证明的缺陷；不要把个人偏好、纯样式建议或未经验证的猜测作为问题发布。
- 发布问题前检查相关实现、调用方、测试、注释和设计约束，确认触发条件真实存在，并排除信任边界误判、忽略既有设计和变量含义误读等常见误报。
- 每条问题只描述一个根因；同一根因影响多处时合并说明，避免重复评论。
- 证据必须包含准确的 `文件路径:行号`、触发条件和可观察后果。GitHub inline thread 已提供行号上下文时，正文仍需说明触发路径或错误行为。
- 验证结果必须与实际执行情况一致；未执行验证时，应明确写出“未执行”及原因。

## 优先级

- `P0`：会造成广泛服务不可用、严重安全事件、不可逆数据损坏或发布完全受阻，且没有可行规避方式。
- `P1`：在现实触发条件下会导致重要功能错误、可靠性或安全性显著下降、数据不一致或严重性能退化，应在合并前修复，但影响范围或紧迫性尚未达到 `P0`。

判定优先级时同时评估影响范围、触发可能性、可规避性和恢复成本，并选择证据能够支持的最高级别。Codex GitHub Review 仅发布确认的 `P0` 或 `P1` 问题；无法证明达到该级别时不发布阻塞性评论，也不要把 `P2`、纯样式建议或 `nit` 提升为 `P1`。

## GitHub 审查意见模板

每条 inline review comment 使用以下格式：

```markdown
**[P0/P1] <中文问题标题>**

- **风险**：<说明用户、数据、系统或发布受到的具体影响>
- **证据**：`<文件路径>:<行号>`；当 `<触发条件>` 时，`<实际行为>`，预期 `<预期行为>`。
- **修复建议**：<给出可执行的最小修复方向，并说明需要补充的验证>
```

未发现需要阻止合并的问题时，统一回复：

```text
未发现需要阻止合并的 P0/P1 问题。
```

## GitHub 审查问题修复回复模板

修复者应在原 GitHub review thread 中回复，避免另开评论导致上下文丢失。根据实际处理结果选择且只选择一种状态。

| 处理状态 | 选择条件 |
| --- | --- |
| `已修复` | 原问题的触发路径和风险已全部消除，且验证结果足以支持结论。 |
| `部分修复` | 已降低风险，但仍有未覆盖场景、残余风险或尚未完成的后续工作。 |
| `不采纳` | 经代码、测试或设计约束核验后确认问题不成立，或团队明确接受该风险且说明依据。 |

### 已修复

```markdown
**处理状态：已修复**

- **修改说明**：<说明修改位置、修复逻辑以及如何消除原风险>
- **验证结果**：`<实际执行的命令>` — <通过结果或关键输出>
- **剩余风险**：无；如仍有剩余风险，应改用“部分修复”状态
- **提交信息**：`<commit SHA>` `<commit subject>`
```

### 部分修复

```markdown
**处理状态：部分修复**

- **修改说明**：<说明已完成的修改及其覆盖范围>
- **未完成原因**：<说明未完全修复的约束或阻塞条件>
- **剩余风险**：<说明仍可触发的场景及影响>
- **后续动作**：<给出负责人、关联 issue/PR 或明确的下一步>
- **验证结果**：`<实际执行的命令>` — <结果；未执行时写明原因>
- **提交信息**：`<commit SHA>` `<commit subject>`；尚未提交时写明原因
```

### 不采纳

```markdown
**处理状态：不采纳**

- **修改说明**：未修改。
- **原因与依据**：<结合实现、测试、设计约束或可复现结果说明不采纳原因>
- **剩余风险**：<说明接受的风险；确认不存在时说明判断依据>
- **后续动作**：<说明无需动作，或给出负责人、关联 issue/PR 和下一步>
- **验证结果**：`<实际执行的命令>` — <支持结论的结果；未执行时写明原因>
- **提交信息**：无代码修改；如有配套提交，填写 `<commit SHA>` `<commit subject>`
```

回复前确认处理状态与实际改动一致，命令、提交 SHA、issue 和 PR 均可追溯。不要只回复“已处理”或“已修复”。
