# Codex 代码审查模板

> 可直接粘贴到 `/review` 的 **Custom review instructions**，也可由 `AGENTS.md` 引用。

## 本次审查参数

- 审查目标：`<PR / 分支 / 提交范围>`
- Base：`<例如 origin/master>`
- 重点关注：`<模块、风险或业务场景；没有则填“无”>`
- 相关测试：`<测试模块或命令；没有则根据变更识别>`

## 审查要求

你正在执行代码审查，只识别并报告本次 diff 引入的可操作问题。

- **只审查，不修改**：不得编辑文件、格式化代码、更新锁文件、提交、推送或创建 PR。
- 先完整阅读仓库根目录 `AGENTS.md`，再阅读变更路径适用的 `.trellis/spec/` 规范。
- 使用指定的 Base 确认审查范围，并检查完整 diff，例如：

  ```bash
  git diff --name-status <base>...HEAD
  git diff <base>...HEAD --
  ```

- 必要时阅读调用方、数据模型、配置、迁移和测试以验证结论，但不要把与本次 diff 无关的既有问题列为发现。
- 不要仅凭推测报告问题；每个发现都必须能由实际代码路径、测试结果或明确契约证明。

## 检查清单

逐项检查：

1. **正确性**：分支逻辑、边界条件、异常路径、返回值和行为契约。
2. **数据一致性**：字段映射、类型、排序/去重、事务、索引与持久化结果。
3. **异步资源与并发**：连接和客户端生命周期、关闭行为、共享状态、竞态、重复执行与幂等性。
4. **配置兼容性**：默认值、环境变量、旧配置、可选字段、启动与升级路径。
5. **安全性**：输入边界、权限、注入、敏感信息和日志泄露；先确认真实信任边界，避免误报。
6. **性能**：无界循环、N+1 请求、重复 I/O、大对象复制、阻塞异步事件循环和明显退化。
7. **测试**：关键成功/失败路径是否覆盖，断言是否有效，测试是否可能在功能失效时仍通过。
8. **项目规范**：`AGENTS.md`、适用 Trellis 规范、依赖锁定、代码风格和类型约束。

## 验证命令

使用仓库锁定的依赖环境运行以下命令，不得用 `uv run --with ...` 临时访问 PyPI：

```bash
uv sync --locked
uv lock --check
uv run ruff check app app_test
uv run pyright app app_test
uv run python -m compileall -q app app_test main.py
uv run python -m app.conf.app_config
uv run python -m app_test.client.test_mysql_client_manager
```

再运行与变更直接相关的定向测试，例如：

```bash
uv run python -m <相关测试模块>
```

MySQL 集成检查需要可用的服务和凭据；服务不可用时，将其列入“环境限制/验证缺口”，不得声称通过。

如果某条命令因网络、代理、权限、工具不可用或审查沙箱限制而失败，必须标记为**环境失败**，保留原始错误，并与代码缺陷分开说明。只有在仓库锁定环境中可复现且能关联到本次 diff 时，才报告为代码问题。正式 Pyright 命令通过时，不得把第三方依赖解析失败误报为代码问题。

## 输出格式

审查结果必须使用简体中文；代码标识符、文件路径、命令、配置键、日志和错误原文保留英文。

每个问题使用以下格式：

```markdown
### [P0|P1|P2|P3] 简明问题标题

- 位置：`path/to/file.py:<line>`
- 证据：指出具体代码路径、契约冲突或可复现结果。
- 触发条件：说明何种输入、状态或执行顺序会触发。
- 影响：说明用户、数据、兼容性、安全性或性能后果。
- 最小修复建议：给出最小且可验证的修改方向，不直接修改代码。
```

优先级含义：

- `P0`：阻断发布或造成广泛、不可接受的严重后果。
- `P1`：高概率导致核心功能错误、数据损坏或安全问题。
- `P2`：在现实条件下会触发的普通功能或可靠性缺陷。
- `P3`：影响较小但值得修复的明确缺陷；不要报告纯风格偏好。

最后列出实际执行的验证命令及结果。若没有发现可报告的问题，明确写：**“未发现需要报告的问题。”**；若存在未能完成的验证，另列“环境限制/验证缺口”，不要将其包装成代码缺陷。
